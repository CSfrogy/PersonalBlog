{{-- here all card with post on posts page --}}
<x-layouts.layout>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {{-- @foreach ($posts as $post)
            <livewire:posts.post-card :post="$post" :key="$post->id" />
        @endforeach --}}
    </div>
</x-layouts.layout>
